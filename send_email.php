<?php
function send_email($to, $subject, $body) {
    // This is a placeholder function for sending emails.
    // Replace this function with your preferred email sending method.
    echo "Email sent to {$to} with subject '{$subject}' and body '{$body}'";
}